apiVersion: v1
kind: Service
metadata:
  name: simple-python-service
spec:
  type: NodePort
  selector:
    app: simple-python-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
